spring:
  application:
    name: recommendation-service

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8085
  servlet:
    context-path: /api/recommendation