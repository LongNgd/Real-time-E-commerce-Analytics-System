spring:
  application:
    name: event-service

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8082
  servlet:
    context-path: /api/event